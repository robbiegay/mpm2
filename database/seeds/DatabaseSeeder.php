<?php

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * @return void
     */
    public function run()
    {
        // factory(App\SynthParam::class, 20)->create()->each(function ($user) {
        //     $user->post()->save(factory(App\Post::class)->make());
        // });
    }
}
